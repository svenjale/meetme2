<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="de.meetme">
    package="com.google.android.gms.location.sample.basiclocationsample" >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".EventApplication"
        android:icon="@drawable/smapicon"
        android:label="Smap"
        android:theme="@style/AppTheme">
        <activity
            android:name=".welcome"
            android:label="Smap"
            android:largeHeap="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login_Activity" />
        <activity android:name=".HelloActivity" />
        <activity android:name=".Profile_Activity" />
        <activity android:name=".createevent" />
        <activity android:name=".eventinfos" />
        <activity android:name=".kontakte" />
        <activity android:name=".kontaktvorschlaege" />
        <activity android:name=".teilnehmerlist" />
        <activity android:name=".anwesendelist" />
        <activity android:name=".profilansicht" />
        <activity android:name=".map" />
        <activity android:name=".help" />
        <activity android:name=".Eventliste_activity" />
        <activity android:name=".profilansichtAndererUser" />
        <activity android:name=".teilnahmenlist" />

        <service
            android:name=".MyAlarmService"
            android:enabled="true" />

        <receiver android:name=".MyReceiver" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".helplogin" />
        <activity android:name=".helpregister" />
        <activity android:name=".helpmain" />
        <activity android:name=".helpevent" />
        <activity android:name=".helpkontakte" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAgT2P8M0lPubru2ZbZjVepvYLsfbTsLn8" />

        <activity android:name=".maphelp"></activity>
    </application>

</manifest>